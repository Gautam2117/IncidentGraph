export default function RetrievalSettingsPage() { return <div className="space-y-6"><header><p className="text-xs uppercase tracking-[0.2em] text-cyan-300">Read-only runtime policy</p><h1 className="mt-2 text-3xl font-semibold text-white">Retrieval configuration</h1><p className="mt-2 text-sm text-slate-400">The console reports deployed retrieval behavior. Infrastructure-controlled values are changed through reviewed configuration.</p></header><div className="grid md:grid-cols-2 gap-4">{[['Vector index', 'pgvector HNSW', 'm=16 · ef_construction=64 · cosine'], ['Lexical index', 'PostgreSQL FTS', 'English websearch query · GIN'], ['Fusion', 'Reciprocal rank', 'Vector + lexical candidate lists'], ['Chunking', 'Bounded overlap', '500 tokens · 50 token overlap']].map(([label, value, detail]) => <div key={label} className="rounded-xl border border-white/10 bg-slate-900/45 p-5"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-lg font-semibold text-white">{value}</p><p className="mt-2 font-mono text-xs text-cyan-300">{detail}</p></div>)}</div></div>; }
