export default function WebhooksSettingsPage() { return <div className="space-y-6"><header><p className="text-xs uppercase tracking-[0.2em] text-cyan-300">Authenticated ingress</p><h1 className="mt-2 text-3xl font-semibold text-white">Webhook integration</h1><p className="mt-2 text-sm text-slate-400">Inbound alert delivery is fail-closed and creates a durable idempotency receipt before incident creation.</p></header><div className="rounded-xl border border-white/10 bg-slate-900/45 p-6"><p className="font-mono text-sm text-cyan-300">POST /api/v1/incidents/webhooks/ingest</p><div className="mt-5 grid md:grid-cols-3 gap-3">{[['X-Webhook-ID', 'Unique delivery identifier'], ['X-Webhook-Timestamp', 'Unix seconds; ±5 minute window'], ['X-Signature', 'HMAC-SHA256(timestamp.payload)']].map(([header, detail]) => <div key={header} className="rounded-lg border border-white/5 bg-slate-950/60 p-4"><p className="font-mono text-xs text-white">{header}</p><p className="mt-2 text-xs text-slate-500">{detail}</p></div>)}</div><p className="mt-5 text-xs text-slate-500">The signing secret is managed outside this interface and is never returned by the API.</p></div></div>; }
