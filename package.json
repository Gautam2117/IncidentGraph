{
  "name": "incidentgraph-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:console": "npm --prefix apps/console run dev",
    "build:console": "npm --prefix apps/console run build",
    "lint": "npm --prefix apps/console run lint",
    "typecheck": "npm --prefix apps/console run typecheck",
    "verify:claims": "python3 scripts/verify_resume_claims.py"
  },
  "workspaces": [
    "apps/*"
  ]
}
